"""
Auth module router stub.
Endpoints for authentication: login, logout, refresh, MFA, SSO, password management.
"""
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()
